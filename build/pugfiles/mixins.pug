mixin footerMenu(name)
    li
        a(href="#")= name

mixin download()
    a(class="download-now-link" href="#") Download now
    a(class="download-link" href="#") or Click here to download

mixin toolHex(id, img)
    .hex-item(id=id)
        .image-block(class=id)
        .bord
